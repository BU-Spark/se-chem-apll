/**
 * Suppress the generic instructor list skeleton while the server loads the
 * complete node editor payload. The editor renders once with all checkpoint
 * and quiz questions already initialized; there is no useful partial UI here.
 */
export default function NodeEditorLoading() {
  return null;
}
